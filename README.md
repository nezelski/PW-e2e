Tallinn Delivery tests project

1.Install dependencies: npm install

2.Install Playwright browsers: npx playwright install --with-deps

3.Create a .env file in the project root: APP_URL=https://fe-delivery.tallinn-learning.ee/signin

4.Add .env file to gitignore

5.Run tests: npx playwright test